{
  "name": "@loom/config",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "pnpm exec tsc -p tsconfig.json",
    "typecheck": "pnpm exec tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/**/*.test.js || true",
    "lint": "echo 'No lint configured for @loom/config'"
  },
  "dependencies": {
    "yaml": "^2.7.1",
    "zod": "^3.24.2"
  }
}
