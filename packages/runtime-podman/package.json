{
  "name": "@loom/runtime-podman",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "pnpm exec tsc -p tsconfig.json",
    "typecheck": "pnpm exec tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/**/*.test.js || true",
    "lint": "echo 'No lint configured for @loom/runtime-podman'"
  },
  "dependencies": {
    "@loom/config": "workspace:*"
  }
}
