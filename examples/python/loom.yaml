version: 1
name: python-example
runtime:
  engine: podman
  rootless: true
  machine:
    managed: true
services:
  app:
    type: python
    image: python:3.12-slim
    workdir: /workspace
    command: |
      mkdir -p public
      if [ ! -f public/index.html ]; then
        printf '%s\n' 'Loom Python example is running.' > public/index.html
      fi
      python -m http.server 8000 --directory public
    ports:
      - "8000:8000"
    volumes:
      - ./:/workspace
  cache:
    type: redis
    image: redis:7-alpine
    volumes:
      - ./data/redis:/data
routes:
  - host: python.loom.local
    service: app
    port: 8000
    https: true
tasks:
  test:
    service: app
    command: pytest -q
