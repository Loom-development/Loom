version: 1
name: php-wordpress-example
runtime:
  engine: podman
  rootless: true
  machine:
    managed: true
services:
  app:
    type: php
    image: ${WORDPRESS_IMAGE:-ghcr.io/loom-development/loom-wordpress@sha256:56b061e053c09046384493bffe0849642856ab413890547ca4b3dd8a121e4013}
    composer: false
    user: root
    userns: keep-id
    entrypoint: ""
    command: |
      set -eu
      target_uid="${HOST_UID:-1000}"
      target_gid="${HOST_GID:-1000}"
      current_gid="$(getent group www-data | cut -d: -f3)"
      if [ "$current_gid" != "$target_gid" ]; then
        groupmod -o -g "$target_gid" www-data
      fi
      current_uid="$(id -u www-data)"
      if [ "$current_uid" != "$target_uid" ]; then
        usermod -o -u "$target_uid" www-data
      fi
      exec docker-entrypoint.sh apache2-foreground
    volumes:
      - ./wp-content:/var/www/html/wp-content
    env:
      WORDPRESS_DB_HOST: ${WORDPRESS_DB_HOST:-db:3306}
      WORDPRESS_DB_NAME: ${WORDPRESS_DB_NAME:-wordpress}
      WORDPRESS_DB_USER: ${WORDPRESS_DB_USER:-wordpress}
      WORDPRESS_DB_PASSWORD: ${WORDPRESS_DB_PASSWORD:-wordpress}
      HOST_UID: ${HOST_UID:-1000}
      HOST_GID: ${HOST_GID:-1000}
    ports:
      - "8090:80"
    healthcheck:
      command: php -r "exit((int)!@fsockopen('127.0.0.1', 80));"
      intervalSeconds: 3
      timeoutSeconds: 3
      retries: 20
      startPeriodSeconds: 30
routes:
  - host: wordpress.loom.local
    service: app
    port: 80
    https: true
