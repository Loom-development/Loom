version: 1
name: spring-boot-example
runtime:
  engine: podman
  rootless: true
  machine:
    managed: true
services:
  app:
    type: java
    image: ${JAVA_IMAGE:-ghcr.io/loom-development/loom-java-21@sha256:0137fa3f265b304b685757d5aea005a81140f833a49f271350b64bc0b349dedf}
    userns: keep-id
    execUser: ${HOST_UID:-1000}:${HOST_GID:-1000}
    workdir: /workspace
    command: mvn spring-boot:run -Dspring-boot.run.jvmArguments="-Dserver.address=0.0.0.0 -Dserver.port=8080"
    ports:
      - "8080:8080"
    volumes:
      - ./:/workspace
    healthcheck:
      command: wget -q -O- http://127.0.0.1:8080/api/health || exit 1
      intervalSeconds: 10
      timeoutSeconds: 10
      retries: 60
      startPeriodSeconds: 30
routes:
  - host: spring-boot.loom.local
    service: app
    port: 8080
    https: true
tasks:
  test:
    service: app
    command: mvn test
